#include <stdio.h>
int main() {
    int n, i, num = 0, maior, posicao;
    for(i = 1; i <= 100; i++) {
        scanf("%d", &n);   
        if(n > num) {
            num = n;
            maior = n;
            posicao = i;
        }
    }
    printf("%d\n%d\n", maior, posicao);
    return 0;
}
