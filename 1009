#include <stdio.h>

int main (void)

{
	char name;
	double salary;
	double sold;
	
	scanf ("%s %lf %lf", &name, &salary, &sold);
	
	double total = ((sold * 0.15) + salary);
	
	printf ("TOTAL = R$ %.2lf\n", total);
	
	return 0;
	
}
